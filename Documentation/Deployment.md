# Deployment

The idea is:

This project should be deployed using a docker file.

Should be hosted on my VPS using docker compose up

When I push to github the github action should ssh into vps and doe a pull and an up.
